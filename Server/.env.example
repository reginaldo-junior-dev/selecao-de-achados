# Apenas para desenvolvimento local (profile dev)
# Valores default já estão em application-dev.properties
# Informe apenas o que for diferente dos defaults

DB_HOST=localhost
DB_PORT=5432
DB_NAME=selecao_de_achados
DB_USERNAME=postgres
DB_PASSWORD=
